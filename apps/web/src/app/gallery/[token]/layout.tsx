import type { Metadata } from "next";
export const metadata:Metadata={title:"Private Client Gallery",robots:{index:false,follow:false}};
export default function PrivateGalleryLayout({children}:{children:React.ReactNode}){return children;}
