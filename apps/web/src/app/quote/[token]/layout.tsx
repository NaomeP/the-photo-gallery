import type { Metadata } from "next";
export const metadata:Metadata={title:"Private Quotation",robots:{index:false,follow:false}};
export default function PrivateQuoteLayout({children}:{children:React.ReactNode}){return children;}
