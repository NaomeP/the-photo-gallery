import type { Metadata } from "next";
export const metadata:Metadata={title:"Private Invoice",robots:{index:false,follow:false}};
export default function PrivateInvoiceLayout({children}:{children:React.ReactNode}){return children;}
